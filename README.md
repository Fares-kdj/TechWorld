# متجر إلكتروني متكامل - My Tech Store
## نظام إدارة متجر إلكتروني احترافي

---

## 📋 نظرة عامة

متجر إلكتروني متكامل لبيع الحواسيب المحمولة والأجهزة الإلكترونية مبني بتقنيات:
- **Backend**: PHP 7.4+
- **Database**: MySQL 5.7+
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **API**: RESTful API

---

## ✨ المميزات الرئيسية

### الواجهة الأمامية (Frontend)
- ✅ تصميم عصري ومتجاوب (Responsive)
- ✅ صفحة رئيسية جذابة مع Hero Section
- ✅ عرض المنتجات بشكل احترافي
- ✅ سلة تسوق عائمة (Floating Cart)
- ✅ صفحة تفاصيل المنتج الكاملة
- ✅ نظام بحث متقدم
- ✅ تصفية المنتجات حسب الفئات
- ✅ **نظام إتمام الطلب (Checkout) متطور**:
    - اختيار الموقع المتسلسل (ولاية -> دائرة -> بلدية)
    - عنوان الشارع اختياري
- ✅ صفحة نجاح الطلب منفصلة (تمنع تكرار الطلب عند التحديث - PRG Pattern)

### لوحة التحكم (Admin Panel)
- 📊 لوحة معلومات شاملة مع إحصائيات
- 📦 إدارة المنتجات (إضافة، تعديل، حذف)
- 📂 إدارة الفئات
- 🛒 إدارة الطلبات وتتبعها (مع تفاصيل الموقع الدقيقة)
- 👥 إدارة العملاء
- 📈 تقارير المبيعات والأرباح
- ⚙️ إعدادات المتجر
- 🔐 نظام تسجيل دخول آمن
- 👨‍💼 إدارة المديرين والصلاحيات

### الأمان
- ✅ حماية من SQL Injection (Prepared Statements)
- ✅ حماية CSRF Token
- ✅ تشفير كلمات المرور (bcrypt)
- ✅ حماية XSS (htmlspecialchars)
- ✅ التحقق من صحة المدخلات

---

## 🚀 التثبيت

### المتطلبات
- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث
- Apache/Nginx Web Server
- PDO Extension
- GD Extension (للصور)

### خطوات التثبيت

#### 1. إعداد قاعدة البيانات

افتح phpMyAdmin أو MySQL CLI واستورد ملف `database.sql`:

```bash
mysql -u root -p < database.sql
```

أو من phpMyAdmin:
- اختر "Import"
- حمّل ملف `database.sql`
- اضغط "Go"

#### 2. إعدادات الاتصال

عدّل ملف `config/database.php`:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_NAME', 'my_tech_store');
```

#### 3. رفع الملفات

انقل المشروع إلى مجلد الويب:
- **XAMPP**: `C:\xampp\htdocs\my-tech-store`
- **WAMP**: `C:\wamp\www\my-tech-store`
- **LAMP**: `/var/www/html/my-tech-store`

#### 4. صلاحيات المجلدات

تأكد من صلاحيات الكتابة:

```bash
chmod -R 755 uploads/
chmod -R 755 assets/
```

#### 5. الوصول للنظام

افتح المتصفح:

**الواجهة الأمامية:**
```
http://localhost/my-tech-store/
```

**لوحة التحكم:**
```
http://localhost/my-tech-store/admin/login.php
```

---

## 🔑 بيانات الدخول الافتراضية

### المدير (Admin)
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

> ⚠️ **هام جداً**: قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول!

---

## 📁 هيكل المشروع

```
my-tech-store/
│
├── admin/                      # لوحة التحكم
│   ├── login.php              # تسجيل الدخول
│   ├── index.php              # الصفحة الرئيسية
│   ├── products.php           # إدارة المنتجات
│   ├── categories.php         # إدارة الفئات
│   ├── orders.php             # إدارة الطلبات
│   ├── customers.php          # إدارة العملاء
│   ├── settings.php           # الإعدادات
│   ├── admins.php             # إدارة المديرين
│   └── reports.php            # التقارير
│
├── api/                       # واجهة برمجية
│   ├── products.php           # API المنتجات
│   ├── orders.php             # API الطلبات
│   └── get_locations.php      # API المواقع (ولايات، دوائر، بلديات)
│
├── config/
│   └── database.php           # إعدادات قاعدة البيانات
│
├── includes/
│   ├── functions.php          # الدوال المساعدة
│   ├── header.php             # الهيدر المشترك
│   └── footer.php             # الفوتر المشترك
│
├── assets/
│   ├── css/
│   │   └── style.css          # ملف الأنماط الرئيسي
│   ├── js/
│   │   └── main.js            # ملف JavaScript الرئيسي
│   └── images/                # الصور
│
├── uploads/                   # مجلد رفع الملفات
│
├── index.php                  # الصفحة الرئيسية
├── products.php               # صفحة المنتجات
├── product.php                # صفحة تفاصيل المنتج
├── checkout.php               # صفحة إتمام الطلب
├── process-order.php          # معالجة الطلب
├── order-success.php          # صفحة نجاح الطلب
├── database.sql               # ملف قاعدة البيانات
└── README.md                  # هذا الملف
```

---

## 🔌 API Documentation

### Base URL
```
http://localhost/my-tech-store/api/
```

### Locations API (New)

#### جلب الولاية / الدائرة / البلدية
```
GET /api/get_locations.php
```

**المعاملات:**
- `type`: نوع البيانات المطلوبة (`wilayas`, `dairas`, `communes`)
- `wilaya_id`: (مطلوب عند طلب `dairas`)
- `daira_id`: (مطلوب عند طلب `communes`)

### Products API

#### 1. جلب قائمة المنتجات
```
GET /api/products.php?action=list
```

**المعاملات:**
- `category` (اختياري): slug الفئة
- `search` (اختياري): كلمة البحث
- `limit` (اختياري): عدد النتائج
- `offset` (اختياري): الإزاحة

**الاستجابة:**
```json
{
  "success": true,
  "data": [...],
  "total": 50
}
```

#### 2. جلب منتج واحد
```
GET /api/products.php?action=get&id=1
```

### Orders API

#### 1. إنشاء طلب جديد
```
POST /api/orders.php?action=create
```

**Body:**
```json
{
  "customer": {
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "phone": "0555123456",
    "address": "شارع الملك فهد",
    "city": "الرياض"
  },
  "items": [
    {
      "product_id": 1,
      "quantity": 1,
      "price": 12999
    }
  ],
  "payment_method": "cash_on_delivery"
}
```

---

## 🎨 التخصيص

### تغيير الألوان

عدّل المتغيرات في `assets/css/style.css`:

```css
:root {
    --primary: #3b82f6;
    --secondary: #8b5cf6;
    --success: #10b981;
    --danger: #ef4444;
}
```

### إضافة لوجو مخصص

استبدل الـ SVG في `includes/header.php` بصورة اللوجو الخاصة بك.

---

## 📝 ملاحظات هامة

### النسخ الاحتياطي
قم بعمل نسخة احتياطية دورية لـ:
1. قاعدة البيانات
2. مجلد `uploads/`

### الصور
- الأنواع المدعومة: JPG, PNG, GIF, WEBP
- الحد الأقصى: 5MB
- الدقة الموصى بها: 800x800 بكسل

### الأمان في الإنتاج
1. استخدم HTTPS
2. غيّر بيانات الدخول الافتراضية
3. حدّث PHP و MySQL بانتظام
4. فعّل الجدار الناري (Firewall)

---

## 🆘 استكشاف الأخطاء

### خطأ: "لا يمكن الاتصال بقاعدة البيانات"
✅ تحقق من `config/database.php`
✅ تأكد من تشغيل MySQL
✅ تأكد من وجود قاعدة البيانات

### خطأ: "لا يمكن رفع الصور"
✅ تحقق من صلاحيات مجلد `uploads/`
✅ تأكد من أن الحجم أقل من 5MB

### خطأ: "Session not working"
✅ تأكد من تفعيل sessions في PHP
✅ تحقق من صلاحيات مجلد sessions

---

## 📞 الدعم والمساعدة

للأسئلة والدعم:
- 📧 Email: support@mytechstore.com
- 🌐 Website: www.mytechstore.com

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

## 🙏 شكر وتقدير

تم بناء هذا المشروع باستخدام:
- PHP
- MySQL
- HTML5, CSS3, JavaScript
- خط Cairo من Google Fonts

---

**تم التطوير بواسطة: فريق التقنية المتطور**  
**الإصدار: 1.1.0**  
**تاريخ التحديث: فبراير 2026**

---

## 🚀 خطط مستقبلية

- [ ] إضافة نظام المراجعات والتقييمات
- [ ] إضافة نظام الخصومات والكوبونات
- [ ] إضافة بوابات دفع إلكتروني
- [ ] تطبيق جوال (iOS & Android)
- [ ] تحسين محركات البحث (SEO)
- [ ] إضافة لغات متعددة
- [ ] نظام النقاط والمكافآت
- [ ] إشعارات البريد الإلكتروني
- [ ] تقارير متقدمة بالرسوم البيانية

---

**نتمنى لك تجربة رائعة مع متجرك الإلكتروني! 🎉**
